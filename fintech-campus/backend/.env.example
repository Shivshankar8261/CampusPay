PORT=4000
JWT_SECRET=change-me-to-a-long-random-string

# Optional: real MongoDB (otherwise the API uses in-memory Mongo — fine for prototyping)
# MONGODB_URI=mongodb://127.0.0.1:27017/fintech_campus

# Set to 0 to force local mongod when MONGODB_URI is unset
# USE_MEMORY_DB=0
